import * as ProfileService from './profile.service';



/**
 * Opcionalmente, exportamos el objeto agrupado para mantener 
 * la organización si se prefiere acceder como UserService.ProfileService.
 */
export { ProfileService };